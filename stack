#include <cstdio>
#include <string>
#include <iostream>
#include <stack>

using namespace std;

stack <int> st;

int main(){
	int N;

	scanf("%d", &N);

	for(int i=0;i<N;i++){
		string s;
		cin >> s;
		if(s == "push"){
			int x;
			scanf("%d",&x);
			st.push(x);
		}
		if(s == "pop"){
			if(st.empty()){
				printf("-1\n");
			}
			else{
				printf("%d\n", st.top());
				st.pop();
			}
		}
		if(s == "size"){
			printf("%d\n", st.size());
		}
		if(s == "empty"){
			if(st.empty()){
				printf("1\n");
			}
			else{
				printf("0\n");
			}
		}
		if(s == "top"){
			if(st.empty()){
				printf("-1\n");
			}
			else{
				printf("%d\n",st.top());
			}
		}
	}

	return 0;
}
